[project]
name = "CryptCloud-Platform"
version = "0.0.1"
description = "Paas 4 Hax0rs"
authors = [
    {name = "cam", email = "cammclain@skiff.com"},
]
dependencies = []
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"


[tool.pdm]
distribution = true
